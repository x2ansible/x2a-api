# Complete Requirements for Enhanced ChefAnalysisAgent with Pattern-Based Extraction

# Core FastAPI and web framework dependencies
fastapi>=0.104.0
httpx>=0.25.0
uvicorn>=0.24.0
gunicorn>=21.0.0
pydantic>=2.4.0

# HTTP client and utilities
requests>=2.31.0

# YAML parsing for configuration
PyYAML>=6.0

# LlamaStack client
llama-stack-client

# MCP (Model Context Protocol) support
fastmcp
fire

# Enhanced regex support for pattern matching
regex>=2022.0.0

# Optional: Additional parsing libraries for future extensibility

# HCL parsing for Terraform support (future)
# python-hcl2>=4.0.0

# Shell script parsing (future)  
# bashlex>=0.18

# JSON schema validation (for config validation)
# jsonschema>=4.0.0

# Performance monitoring (optional)
# psutil>=5.9.0

# Async file operations (optional)
# aiofiles>=23.0.0

# Installation Instructions:
#
# 1. Basic installation (required):
#    pip install -r requirements.txt
#
# 2. Verification:
#    python -c "import re; print('Pattern extraction ready')"
#
# 3. Testing Chef parsing specifically:
#    python -c "
#    import re
#    content = 'package \"nginx\" do\n  action :install\nend'
#    matches = re.findall(r'package\s+[\"\\\']([^\"\\\']+)[\"\\\']', content)
#    print('Chef parsing OK:', matches)
#    "
#
# 4. Testing YAML parsing for Ansible (future):
#    python -c "
#    import yaml
#    content = '---\n- name: Install nginx\n  package:\n    name: nginx'
#    data = yaml.safe_load(content)
#    print('YAML parsing OK:', data)
#    "
#
# Note: Pattern-based extraction uses only Python standard library
# No native compilation or external dependencies required